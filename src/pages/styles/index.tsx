import { Typography } from 'antd';
import styled from '@emotion/styled';

const PurpleTitle = styled(Typography.Title)`
  text-color: #993366 !important;
  font-weight: 600 !important;
`;

export default PurpleTitle;
